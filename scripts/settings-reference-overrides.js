'use strict';

// Purpose copy for schema entries that are subordinate configuration/data
// fields rather than independently registered features. The generated README
// reference uses canonical feature descriptions wherever they exist and falls
// back to these source-audited explanations for the remaining entries.
const PURPOSE_OVERRIDES = Object.freeze({
    openThumbnailButton: 'Adds a View button beside the thumbnail download button that opens the full-size thumbnail in a new tab. It shares the download resolver, so a video with no max-res thumbnail opens the best one it has. Needs the Download Thumbnail Button feature on, since it sits beside it.',
    channelLandingTab: 'Which channel tab "Channels to Videos Tab" opens. Applies only while that feature is on, and falls back to Videos when the channel does not carry the chosen tab.',
    selectorAutoRefresh: 'Checks once a day for an updated page-matching rule set, through the same signed fetch, digest verification and rollback the manual Refresh button uses. Off by default, and no request is made while it is off.',
    uiStyle: 'Selects the global corner treatment: square applies Astra\'s squaring rules, while rounded preserves rounded surfaces.',
    colorTheme: 'Applies a built-in dark-mode color palette across YouTube; none leaves the native palette unchanged.',

    quickLinkItems: 'Defines up to ten masthead quick links, one Label and YouTube path per line; unsafe schemes and non-YouTube destinations are rejected.',
    alternativeFrontendInstance: 'Sets the HTTPS frontend origin used when Open in Alternative Frontend builds an external video link.',
    hiddenGuideElements: 'Lists the individual Guide and mini-Guide destinations hidden by Hide Guide Elements.',

    hiddenActionButtons: 'Lists watch-page action buttons hidden while the Action Buttons manager is enabled.',
    hiddenPlayerControls: 'Lists native player controls hidden while the Player Controls manager is enabled.',
    hiddenWatchElements: 'Lists watch-page sections and actions hidden while the Watch Elements manager is enabled.',
    videoNotesData: 'Stores local note records keyed by video ID for Video Notes import, export, and recovery.',
    transcriptPreferredLanguage: 'Chooses the preferred transcript caption language; auto follows the browser language before the English and first-track fallbacks.',

    autoSkipChapterPatterns: 'Provides comma-separated chapter-name fragments that Auto-Skip Chapters treats as skippable.',
    persistentSpeedValue: 'Sets the playback rate reapplied by Persistent Playback Speed.',
    featureSchedules: 'Maps a feature id to an optional active window ({ start, end, days, enabled }) evaluated against the local clock, so a toggle can switch itself on during focus hours and hand back your previous value when the window closes.',
    heatmapSmartSpeedColdRate: 'Sets the playback rate Heatmap Smart Speed uses through the parts of a video nobody rewatches. Never slows playback below your own speed.',
    searchFilterSort: 'Chooses the sort filter automatically added to search URLs that do not already contain a YouTube filter.',
    rememberVolumeLevel: 'Stores the logical volume percentage restored by Remember Volume.',
    autoSubtitleLang: 'Chooses the preferred BCP-47 caption track after Auto Subtitles enables captions; exact matches win before primary-language matches.',
    videoRotationAngle: 'Sets the clockwise rotation applied by Video Rotation.',
    videoFlipMode: 'Chooses the horizontal or vertical transform applied by Video Flip.',
    wheelSeekStepSec: 'Sets how many seconds Wheel Seek moves for each wheel step over the progress bar.',
    perChannelIntroOutroData: 'Stores per-channel intro and outro offsets used by Per-Channel Intro/Outro skipping.',
    vvfBrightness: 'Sets Video Visual Filters brightness as a percentage of the original image.',
    vvfContrast: 'Sets Video Visual Filters contrast as a percentage of the original image.',
    vvfSaturation: 'Sets Video Visual Filters saturation as a percentage of the original image.',
    vvfHue: 'Sets Video Visual Filters hue rotation in degrees.',
    vvfGrayscale: 'Sets the grayscale percentage applied by Video Visual Filters.',
    vvfSepia: 'Sets the sepia percentage applied by Video Visual Filters.',
    preferredAudioLang: 'Sets the preferred BCP-47 primary language used by Audio Track Language selection.',

    qualityDefaultNormal: 'Sets the quality target used by Per-Context Quality in the normal watch-page layout.',
    qualityDefaultTheater: 'Sets the quality target used by Per-Context Quality in theater mode.',
    qualityDefaultFullscreen: 'Sets the quality target used by Per-Context Quality in fullscreen.',
    qualityDefaultBackground: 'Sets the quality target used by Per-Context Quality while the tab is in the background.',
    qualityDefaultEmbed: 'Sets the quality target used by Per-Context Quality on embedded players.',

    hideVideosKeywordFilter: 'Provides comma-separated title or channel terms that Video Hider matches locally.',
    hideVideosFilterListUrl: 'Sets the exact HTTPS URL for the optional remote Video Hider rule subscription; the host requires an explicit browser grant.',
    hideVideosDurationFilter: 'Hides videos shorter than this many minutes; zero disables the duration rule.',
    hideVideosSubsLoadLimit: 'Stops subscriptions-feed continuation loading after repeated mostly-hidden batches to prevent an endless fetch loop.',
    hideVideosSubsLoadThreshold: 'Sets how many consecutive mostly-hidden subscriptions batches trigger the load limiter.',
    hideVideosSubsLoadHiddenRatio: 'Sets the fraction of a subscriptions batch that must be hidden before it counts toward the load-limiter streak.',
    hideVideosRemoveHiddenCards: 'Removes matched cards from the current feed DOM instead of only collapsing them with CSS.',
    hideVideosShowFilterReason: 'Shows a local placeholder explaining which Video Hider rule hid each matched card.',
    hideVideosShowQuickHideButton: 'Shows the per-thumbnail X control for one-click local video hiding.',
    markWatchedVideos: 'Adds a per-card local mark-as-watched control; marked cards dim or are removed when hidden-card removal is enabled.',
    hideVideosAllowChannelBlock: 'Allows right-clicking a thumbnail hide control to add its channel to the active channel list.',
    hideVideosChannelAllowlist: 'Switches channel filtering from a blocklist to an allowlist; an empty allowlist applies no channel filter.',
    hideVideosRememberRestoredVideos: 'Adds restored videos to Allowed Videos so another matching rule does not immediately hide them again.',
    hideVideosScopeHome: 'Runs Video Hider rules and quick actions on the YouTube home feed.',
    hideVideosScopeSubscriptions: 'Runs Video Hider rules and bulk actions on the subscriptions feed.',
    hideVideosScopeSearch: 'Runs Video Hider video and channel rules on search results.',
    hideVideosScopeWatch: 'Runs Video Hider rules on recommendations beside and below the player.',
    hideVideosScopeChannels: 'Runs Video Hider rules on channel pages.',
    hideVideosScopeOther: 'Runs Video Hider rules on supported feed surfaces outside the named scopes.',
    hideVideosLowViewFilter: 'Enables hiding cards whose exposed view count is below the configured threshold.',
    hideVideosLowViewThreshold: 'Sets the minimum visible view count accepted by the low-view filter.',
    hideVideosHideLive: 'Hides cards identified as currently live streams.',
    hideVideosHideUpcoming: 'Hides scheduled, upcoming, and reminder-driven cards.',
    hideVideosHideMixes: 'Hides radio-style and auto-generated YouTube Mix cards.',
    hideVideosHidePlaylists: 'Hides playlist and multi-video cards on supported surfaces.',
    hideVideosHideMovies: 'Hides rental, purchase, free-with-ads, and movie-labelled cards.',
    hideVideosHideAutoDubbed: 'Hides cards labelled as dubbed, auto-dubbed, or alternate-audio videos.',
    hideVideosWatchedRatio: 'Hides cards whose visible progress reaches this percentage; zero disables the rule.',
    hideWatchedMode: 'Chooses whether Hide Watched Videos dims matched cards or removes them from view.',
    advancedLocalPredicate: 'Enables the bounded local predicate evaluator for advanced card filtering without remote code execution.',
    advancedLocalPredicateCode: 'Stores the local predicate expression evaluated against the documented, read-only video-card context.',

    reactionSpammerMinIntervalMs: 'Sets the minimum delay between Reaction Spammer actions; runtime enforcement never allows less than 500 ms.',
    hiddenChatElements: 'Lists live-chat surfaces hidden while the Chat Elements manager is enabled.',

    subscriptionViewMode: 'Chooses grid, list, or compact presentation for the subscriptions feed view controls.',
    subscriptionOrderMode: 'Chooses native feed order or newest-first ordering across cards already loaded in the page.',
    subscriptionGroupData: 'Stores subscription groups, nested parent links, channel memberships, colors, and per-group sort modes.',
    subscriptionSortMode: 'Sets the fallback subscription sort when no active group supplies its own sort mode.',
    subscriptionShowNewSinceLastVisit: 'Shows per-channel new-since-last-visit badges in Subscription Groups.',
    subscriptionLastVisitData: 'Stores bounded per-channel visit timestamps used to calculate new-since-last-visit counts.',
    subscriptionUnsubscribeStagingData: 'Stores stale-channel unsubscribe candidates during their 30-day review and recovery window.',
    subscriptionAiTags: 'Generates local group tags with the browser built-in Summarizer when that capability is available.',
    subscriptionAiTagData: 'Stores generated subscription-group tags and generation timestamps locally.',
    subscriptionFilterLive: 'Filters currently live cards from the extension subscription view controls.',
    subscriptionFilterStreamed: 'Filters completed livestream recordings from the extension subscription view controls.',

    sbPerChannelProfilesData: 'Stores extension-only SponsorBlock category overrides keyed by channel.',
    returnDislikeCacheHours: 'Sets how many hours Return YouTube Dislike data remains fresh in the local cache.',
    returnDislikeShowRatio: 'Shows the estimated like-to-dislike ratio alongside Return YouTube Dislike counts.',
    deArrowChannelOverrides: 'Stores per-channel DeArrow modes so a channel can use replacements, originals, or disable processing.',

    dwBreakIntervalMin: 'Sets the continuous-watch interval after which Digital Wellbeing prompts for a break; zero disables break prompts.',
    dwDailyCapMin: 'Sets the daily watch-time cap enforced by Digital Wellbeing; zero disables the cap.',
    dwWatchTimeToday: 'Stores the dated, merge-safe daily watch-time accumulator used by Digital Wellbeing.',
    shortsWatchTimeToday: 'Stores the dated Shorts budget ledger and any active snooze deadline. Settings shows the current-day value as read-only status.',
    aiSummaryModel: 'Sets the provider-specific model identifier sent with AI video-summary requests.',
    transcriptAiTarget: 'Chooses the external AI workspace opened by Transcript to AI Handoff after copying the prepared transcript.',

    sidebarOrder: 'Stores the user-defined settings-sidebar category order; null uses the shipped order.',
    privacyDataFlowPanel: 'Shows the popup data-flow inspector with every API origin and removable runtime host grant.',
    syncSettings: 'Opts the extension into browser-account sync for the bounded safe preference and Video Hider payload.',
    safeStoreProfile: 'Keeps the effective profile store-safe, hides GitHub-full controls, and scrubs full-profile values from exports.',
    githubFullProfile: 'Unlocks GitHub-full settings and their explicitly granted optional network capabilities in a compatible build.',
    syncSafePrefs: 'Includes the approved safe preference subset in safe-store profile exports and browser sync.',
    syncSafePrefsAllowlist: 'Lists the schema keys eligible for the bounded safe-preference sync payload; unknown and unsafe keys are discarded.',

    apiRetryBackoff: 'Enables bounded exponential backoff for retryable external API requests so a failing service is not hammered.'
});

module.exports = { PURPOSE_OVERRIDES };
