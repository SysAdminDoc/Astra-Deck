node_modules/
.venv/
.release-venv/
build/
*.pem
*.mhtml
*.bak
comments.png
/YTKit-v1.2.0.user.js

# Authenticated selector-capture state is local, sensitive, and must never be
# staged. Use an external Chrome profile for account-gated MHTML captures.
.auth/
playwright/.auth/
capture-profiles/
*.storageState.json

# AstraDownloader.exe belongs to SysAdminDoc/AstraDownloader, which builds and
# releases it (the companion left this repo in a6bb685f). Astra Deck releases
# must never carry it — `npm run release:readiness` fails on a staged copy.
# Ignored so a stray local build cannot be committed here.
AstraDownloader.exe
CLAUDE.md
CODEX-CHANGELOG.md
CODEX_CHANGELOG.md
CLAUDE-HANDOFF-PROMPT.md
CONTINUATION_PROMPT.md
.claude/
.claude-octopus/
.codex/
.factory/
docs/research/

# Internal process / working notes — kept local, not part of the published repo.
AGENTS.md
AUTONOMOUS-LOOP-STATE.md
COMPLETED.md
RESEARCH_REPORT.md
docs/audit/
docs/research-cycle-*.md
docs/research-feature-plan-*.md
docs/archive/research/
docs/archive/roadmap-dossier-*.md

# Python build artifacts (the companion's own tree lives in another repo now;
# *.pyc still applies to any local tooling run from scripts/)
*.pyc

# Local-only working notes (never track). Public product docs — README,
# CHANGELOG, INSTALL, SECURITY, ROADMAP, docs/ — stay tracked: CI tests pin
# them and the store listings require a reachable privacy policy.
TODO.md
NOTES.md
AUDIT*.md
SESSION*.md

# AI config
.ai/
PROJECT_CONTEXT.md

# Chrome-generated DNR ruleset metadata (created on unpacked load)
extension/_metadata/

# Local visual-polish baselines and ImageGen targets. These are working proof,
# not release assets.
.mockups/
